apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/{{.ORG}}/activiti-cloud-connector:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/{{.ORG}}/activiti-cloud-connector:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: activiti-cloud-connector
        chartPath: charts/activiti-cloud-connector
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/{{.ORG}}/activiti-cloud-connector"
          image.tag: "{{.DIGEST_HEX}}"
